name = "my-docs-viewer"
compatibility_date = "2024-09-23"
pages_build_output_dir = "./build"

# ──────────────────────────────────────────────────────────────
# 在 Cloudflare Dashboard 或通过 wrangler secret put 设置以下变量
# 不要把真实密码硬编码在这里！
#
# wrangler pages secret put AUTH_PASSWORD --project-name=my-docs-viewer
# wrangler pages secret put AUTH_SECRET   --project-name=my-docs-viewer
# ──────────────────────────────────────────────────────────────
